{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter car-tracker-backend build && pnpm --filter car-tracker-frontend build && node scripts/verify-vercel-output.cjs",
  "outputDirectory": "dist",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}